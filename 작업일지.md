# VibeMoji 작업일지

> 자연어 → 32장 이모티콘 자동 생성 + 멀티 플랫폼 패키징 웹앱

---

## 2026-05-07 (DAY 5, 추가 2) — 무료 + 좋은 퀄리티 경로 (Pollinations + Gemini Direct)

### 🎯 사용자 요구
"무료로 좋은 퀄리티를 낼 수는 없어?" → "둘 다 해"
→ Blaze/Functions 없이 클라이언트 사이드에서 직접 AI 호출하는 경로 2종 동시 구축.

### 💡 핵심 발견
- Cloud Functions를 거치지 않고 **브라우저 → AI API 직접 호출**이 가능
- 두 무료 경로:
  - **Pollinations.ai**: 키 없이 즉시. Flux 모델, 운영자 0원 / 사용자 0원
  - **Gemini Direct (BYOK)**: 사용자 본인 키로 브라우저에서 직접 호출. Nano Banana 동일 퀄리티, 무료 티어 분당 ~10회
- 결과: Blaze 가입 안 해도 32장 캐릭터 자동 생성 동작

### ✅ 신규 라이브러리 (`lib/ai/`)
- `types.ts` — `AIGenerator` 인터페이스, `GenerationInput`, blob↔base64 헬퍼
- `pollinations.ts` — `<img crossOrigin="anonymous">` 로드 → canvas drawImage → toBlob 패턴
- `gemini-direct.ts` — `generativelanguage.googleapis.com` 직접 fetch + BYOK 키 사용
- `throttle.ts` — RPM 제어 (Gemini 6.5초, Pollinations 1초)
- `index.ts` — 어댑터 카탈로그 + provider 디스패치
- 운영자 비용: 0원 / 사용자 비용: 0원 (무료 티어 안에서)

### ✅ 32 감정 데이터 분리 (`lib/emotions.ts`)
- `EMOTIONS_32`: 32개 슬롯 한글 라벨 + 영문 action description (evals/datasets/emotions.yaml과 동일)
- `buildSeedPrompt(description)`, `buildVariantPrompt(description, emotion, hasReference)`
- reference 지원/미지원 별로 다른 prompt 전략

### ✅ generate 페이지 완전 재작성 (`app/generate/page.tsx`)
- **provider 토글 3개**: 🌸 Pollinations / ✨ Gemini (BYOK) / 🎭 Mock
- Mock 모드는 그대로 유지 (UX 미리보기용)
- 시드 단계: 1번 호출 → 1장 표시 (재생성 가능)
- 32종 단계: throttle 사이로 32번 순차 호출, 진행 progress bar
- 슬롯 클릭 → 개별 PNG 즉시 다운로드 (blob URL)
- "전체 ZIP 다운로드" 버튼: 시드 + 32장을 JSZip으로 묶어 한 번에
- 호출 로그 details (시간/크기/실패 사유)
- 중지 버튼 + 타임아웃 (Pollinations 60초)

### 🚀 사용자 경험 변화
| 이전 | 이후 |
|------|------|
| 32 슬롯 모두 같은 🐰 emoji | 진짜 32종 캐릭터 PNG (Pollinations 즉시 / Gemini 자기 키) |
| Cloud Functions 배포 필요 | 브라우저에서 바로 동작 |
| Blaze 결제 필수 | 운영자/사용자 모두 0원 |
| AI 퀄리티 = 0 | Pollinations(중상) / Gemini(최상) 선택 |

### 📊 빌드/테스트
- `npm run build` (GITHUB_PAGES=true): ✅ **17 정적 페이지** (변동 없음, 모두 client side)
- `npm run test`: ✅ **19/19**

### 🚧 한계 명시
- Pollinations는 reference image 미지원 → 캐릭터 일관성은 prompt에만 의존. 단순한 캐릭터(분홍 토끼 등)는 OK, 복잡한 디테일은 매번 다르게 그려질 수 있음
- Gemini 무료 티어: 분당 10회 한도 → 32장 약 3-4분 (throttle로 자동 대기)
- Pollinations 가끔 응답 지연 → 60초 타임아웃 + 슬롯별 실패 표시

---

## 2026-05-07 (DAY 5) — 발행 직전 마감 작업 (에디터 테두리 / 카카오 압축 / 자료 제보 / 룰 배포)

### 🎯 오늘의 목표
DAY 4 끝에 남겨둔 다음 단계 후보 6건 중 4건 처리해서 **발행 가능 상태**까지 완성.

### ✅ 완료한 작업

#### 1. Firestore 룰 라이브 배포 (BYOK + public 화이트리스트)
- BYOK용 `credits == 5` 룰 라이브 반영
- `public/{topic}/{collection}/{itemId}` 패턴 추가 — `topic` 화이트리스트(`waitlist`, `resource_submissions`)로만 create 허용, 페이로드 필드 30개 미만, `createdAt: timestamp` 필수
- 이전엔 catch-all `match /{document=**}` 차단에 베타 대기열 폼이 막혔을 가능성 → 이번 배포로 정상화
- 배포 명령: `firebase deploy --only firestore:rules --project vibemoji-app` ✅ (총 2회: BYOK 직후 + public 화이트리스트 추가 후)

#### 2. Functions 배포 시도 → Blaze 게이트 재확인
`firebase deploy --only functions` 결과:
> Your project vibemoji-app must be on the Blaze (pay-as-you-go) plan
> Required API: cloudbuild.googleapis.com, artifactregistry.googleapis.com

→ 사용자 결제 수단 등록 전엔 BYOK 분기 함수 + onUserCreate 5크레딧 룰이 코드상으로만 존재. 베타에는 회원가입 시 클라이언트 `ensureUserDocument` 폴백(5)에서 동작하므로 실사용 영향 없음.

#### 3. 에디터 "🖼️ 테두리 적용" 버튼 통합 (`/editor`)
- `app/tools/_lib/outline.ts`의 `applyOutline(canvas, opts)` 직접 import
- 헤더에 `btn-accent` "🖼️ 테두리" 버튼 → 클릭 시 모달 오픈
- 모달 옵션:
  - 두께 슬라이더 1~20px (4px = 카카오 권장 표시)
  - 색상 5종 프리셋(흰/검/핑크/노랑/보라) + 커스텀 컬러 피커
- 적용 흐름:
  1. 활성 레이어 알파 픽셀 0개면 가드 alert
  2. `history.commit(snapshotFrame())` — undo 가능
  3. `applyOutline(layerCanvas, { thickness, color, alphaThreshold: 16 })`
  4. 결과 캔버스를 활성 레이어에 `clearRect + drawImage`로 다시 그리기
  5. `composite()` + `filledSlots`에 활성 슬롯 추가
- → 작가가 G펜으로 캐릭터 그린 뒤 1클릭으로 두꺼운 흰 외곽선 자동 추가. 이비스페인트의 "스타일 → 스트로크 → 외부폭 4" 단축.

#### 4. 사이즈 변환기 카카오 ≤150KB 자동 압축 (`/tools/resize`)
- 새 라이브러리 `app/tools/_lib/compress.ts`:
  - `compressPngTo(canvas, maxBytes)` — 8단계 점진적 양자화
  - 단계: 원본 → RGB 7→6→5→4→3→2→1비트 + 알파 임계화(0~200)
  - 카카오 360×360 기준 보통 레벨 2-3에서 ≤150KB 도달 (단순 이모티콘은 레벨 0-1로 통과)
  - 라운딩 절반값 더해 양자화 시 색상 시프트 최소화
- UI 변경:
  - 사이드 패널에 "🗜️ 카카오 ≤150KB 자동 압축" 토글 (기본 ON)
  - ZIP 생성 시 `kakao-static` 플랫폼만 `compressPngTo` 적용, 다른 플랫폼은 원본 PNG 그대로
  - 결과 영역에 "🗜️ 카카오 압축 로그 (N)" details — 파일별 크기·레벨·RGB 비트·α임계 표시
- → 카카오 사양 자동 충족. 사용자가 별도 압축 도구를 거칠 필요 없음.

#### 5. 신규 자료 제보 폼 (`/resources`)
- `components/ResourceSubmissionForm.tsx` 신규:
  - 필드: 자료 이름 / URL / 카테고리(브러시·PDF·이미지·도구·튜토리얼·기타) / 원작자 / 연락 이메일 / 설명 / 동의 체크박스
  - 검증: URL `http(s)://` 시작, 제목 2자 이상, 동의 필수
  - 필드별 길이 제한(80~500자) — 어뷰즈 방지
  - Firestore `public/resource_submissions/items/{auto}`에 `serverTimestamp()` 포함 저장
  - 성공 시 알림 + 폼 초기화, 실패 시 에러 메시지
- `/resources` 페이지 "제보 환영" 정적 카드를 실제 동작 폼으로 교체

### 📊 빌드/테스트
| 검증 | 결과 |
|------|------|
| `npm run build` (GITHUB_PAGES=true) | ✅ **17 정적 페이지** |
| `npm run test` | ✅ **19/19** (outline 9 + 메트릭 10) |
| Functions 빌드 (tsc) | ✅ |
| Firestore 룰 배포 | ✅ 라이브 |

### 🚧 남은 다음 단계 후보 (DAY 4에서 이월)
- [ ] 카카오 큰 이모티콘 비율별 trim 옵션 (현재 540×540/540×300/300×540 contain 모드만)
- [ ] 비트윈 제안서 템플릿 (이메일 자동 생성)
- [ ] OGQ 메인/탭 자동 패딩 위치 조정 (캐릭터 중심 자동 검출)
- [ ] (Blaze 후) Functions 배포 + AI 실호출 동작 검증

### 💡 사용자 워크플로 영향
- 에디터 "테두리" 버튼 + 카카오 ≤150KB 압축이 합쳐져서 **에디터에서 그림 → 1클릭 외곽선 → 사이즈 변환기에서 1클릭 ZIP**으로 카카오 사양 100% 자동 충족
- 외부 자료 제보 폼이 동작하면서 베타 사용자 → 큐레이션 자료 자체 확장 루프 형성

---

## 2026-05-07 (DAY 4) — 작가 커뮤니티 도구 통합 + 리소스 큐레이션

### 🎯 오늘의 목표
- 동동작가 등 외부에서 공유된 도구 7건을 분석해 우리 레포에 자체 구현 가능한 부분을 흡수
- 회원가입 없이 바로 쓰는 "도구함" 섹션을 만들어 신규 사용자 진입 장벽 낮추기

### ✅ 완료한 작업

#### 1. 외부 도구 7건 조사
- 🎨 PNG 흰색 테두리 생성기(png-outline-five.vercel.app) — 두께·영역·미리보기 배경 옵션 확인
- 📐 이모티콘 사이즈 자동 변환기(dongdong-emoticon-size-converter.vercel.app) — 카카오 360 / OGQ 740×640 / LINE 370×320 지원, 단일 PNG · ZIP 미지원
- 표정 가이드 9 PNG + PDF (Drive 폴더)
- 캐릭터 도장 brushset 3종 + PDF (Drive 폴더)
- 자세 9종 브러시 (Drive 폴더)
- 합격 캔버스 세팅 PDF (Drive)

#### 2. 흰색 테두리 생성기 자체 구현 (`/tools/outline`)
- `app/tools/_lib/outline.ts` — Felzenszwalb-Huttenlocher **2-pass squared euclidean distance transform** (O(W·H))
  - 큰 이미지(2000px+)에서도 즉시 처리
  - 0~1 픽셀 거리 안티앨리어싱 마스크로 가장자리 부드럽게
  - 색·임계값·영역(전체/사각형) 전부 옵션화
- 페이지 기능: 드래그 업로드 / 두께 1~40px / 색상 / 영역 드래그 지정 / 체커·밝게·어둡게 미리보기 / PNG 다운로드
- 차별점: 원본 대비 **다중 색 + 영역 지정 + 알파 안티앨리어싱**

#### 3. 사이즈 변환기 자체 구현 (`/tools/resize`)
- `app/tools/_lib/resize.ts` — 6개 플랫폼 17개 타겟 정의
  - 카카오 360×360
  - OGQ 740×640 + 메인 240×240 + 탭 96×74
  - LINE 370×320 + 메인 240×240 + 탭 96×74
  - Etsy 1000×1000
  - Redbubble 4500×5400 (POD)
  - 미리캔버스 1024×1024
- 비율 모드: contain(여백) / cover(잘라내기) / stretch(늘림)
- **다중 PNG 일괄 업로드 → JSZip으로 ZIP 묶음 다운로드**
- 차별점: 원본 대비 **6개 플랫폼 + 다중 파일 + ZIP 일괄 다운로드**

#### 4. 외부 자료 큐레이션 페이지 (`/resources`)
- 6개 자료 카드: 표정 가이드, 도장 브러시, 자세 브러시, 캔버스 PDF, 원본 테두리/리사이즈 도구
- 각 카드에 출처 명시 + "VibeMoji와 함께 쓰는 법" 인사이트
- 72시간 한정 공유 안내 노출

#### 5. 도구함 인덱스 + 통합
- `/tools` 인덱스 페이지 (3개 도구 카드)
- NavBar에 "도구함 ▾" 드롭다운 메뉴
- 모바일 NavBar에 도구함 탭 추가
- 랜딩 페이지에 "회원가입 없이 바로 쓰는 도구함" 섹션 신설
- sitemap.ts에 `/tools`, `/tools/outline`, `/tools/resize`, `/resources` 4개 경로 추가

### 📦 의존성 변경
- `jszip ^3.10.1` 추가 (`npm install` 필요)

### 🆕 platforms.ts / resize.ts 사양 보강 (캔버스 세팅 PDF 직접 분석)
PDF 1건(`canvas-setup.pdf`)을 직접 받아 정확한 사이즈/장수 표 확보. 나머지 3건(표정/도장/자세 PDF)은 **Fasoo DRM**으로 보호되어 로컬 텍스트 추출이 불가 — 외부 큐레이션 링크로만 안내. PNG 가이드 9장(1080×1080 RGBA)은 다운로드해 한 장 시각 확인(투명 배경 + 양볼 핑크 + 단순 검은 라인 표정).

플랫폼 데이터에 추가된 7건:
- **카카오 큰 이모티콘** — 16개, 기본 540×540 / 가로 540×300 / 세로 300×540
- **카카오 미니 멈춤** — 42개, 180×180
- **카카오 미니 움직임** — 35개, 180×180
- **LINE 이모티콘 (Emoji)** — 일반 8~40개 / 문자 104개, 180×180 (기존 LINE 스티커는 별도 유지)
- **비트윈(Between)** — 정지 16/28/30 · 동 12/16/24 set, 512×512, 이메일 제안
- (확인) BAND Sticker Shop — 신규 제안 중단 → OGQ로 일원화. 추가 안 함.

`resize.ts` 변환 타겟:
- 카카오 큰: 540×540 / 540×300 / 300×540
- 카카오 미니: 180×180
- LINE 이모티콘: 180×180
- 비트윈: 512×512

→ 사이즈 변환기로 한 번에 변환 가능한 플랫폼이 6개 → **9개 / 17 → 22 타겟**으로 증가.

### 🚧 막힌 부분 — Fasoo DRM (해결)
- 표정/도장/자세 PDF는 Fasoo DRM 적용. 사용자 PC에서 인쇄→PDF로 다시 저장(B방식)해도 DRM 마크가 그대로 부착됨 — 회사 보안 정책이 인쇄 출력까지 가로채는 구성으로 보임.
- Drive `thumbnail?id=...` 1페이지(표지)만 인증 없이 받혀짐. 내부 페이지부터는 OAuth 토큰 필요.
- **해결**: 사용자가 PDF를 페이지별 JPG로 추출(`도장브러쉬_images/`, `가이드png_images/`) → 일반 이미지라 DRM 없이 직접 분석 가능. `Read`로 21장 모두 흡수.
- 사이드 발견: **표정 폴더와 자세 폴더 안의 사용법 PDF는 동일 파일**(Drive 썸네일 80,602 bytes 정확 일치). 정식 명칭 = "동동작가가 알려주는 가이드 PNG 사용법 — 프로크리에이트 ver." → /resources 카드 6→5개 통합.

### 📚 PDF 21페이지에서 흡수한 인사이트
**가이드 PDF (PNG 사용법) — 9p**
- 자료 분류: **AI 활용 이모티콘 가이드 템플릿 9종 (0413)** = 활용 도장 브러쉬 모음 + AI 활용 동작 3종 + AI 활용 표정 3종 + AI 활용 효과 3종 (4×3 매트릭스 구조).
- 권장 워크플로: 사진 삽입 → 위치/크기 조절 → 레이어 **불투명도 30%** → 새 레이어에 캐릭터.

**도장 브러쉬 PDF — 12p**
- **공식 작업 캔버스: 1080×1080 px @ 72 dpi** — 우리 사이즈 변환기/리소스 카드에 권장사양으로 노출.
- brushset 3종 = 일체형 / 사족보행형 / 기본형.
- 라이브러리에 추가 자료 단서: '필수 동작 가이드 101종', '동동 브러쉬'(별도 자료).
- 4단계 도장 제작 워크플로: ① 캐릭터 그리기 → ② 도장 브러시 복제 → ③ 모양 편집기에 캐릭터 PNG 등록(자동 흑백) → ④ 개인 도장 브러시로 비율·구도 잡기.

### 🔧 PDF 흡수 후 코드 반영
- `lib/platforms.ts` — 카카오 큰/미니, LINE 이모티콘, Between까지 7종 추가(이미 반영)
- `app/tools/_lib/resize.ts` — 카카오 ‘작업 캔버스 1080×1080 @72dpi 권장’ 설명 추가
- `app/resources/page.tsx`
  - 표정 카드 → "AI 활용 이모티콘 가이드 템플릿 9종" 정식 명칭 + 30% 불투명도 워크플로 명시
  - 도장 카드 → 4단계 도장 제작 절차 직접 텍스트로 풀어 적음
  - 캔버스 카드 → 1080×1080 표준 + 사이즈 변환기로 직결되는 인라인 링크
- `app/editor/page.tsx` — 트레이스 슬라이더 옆 **`30% 권장`** 원클릭 버튼 추가 (동동 작가 워크플로와 우리 도구의 자연스러운 다리)

### 🧪 다음 단계 후보
- [ ] 에디터 우상단에 "테두리 적용" 버튼 — `applyOutline`을 활성 슬롯 캔버스에 직접 적용
- [ ] 사이즈 변환기에 "카카오 ≤150KB 압축" 옵션 (Canvas → quality 단계별 검증)
- [ ] 카카오 큰 이모티콘은 가로/세로/기본 비율이 달라 contain 모드 외에 별도 trim 옵션 필요
- [ ] 비트윈 제안서 템플릿 (이메일 자동 생성)
- [ ] OGQ 메인/탭 자동 패딩 위치 조정 (캐릭터 중심 자동 검출)
- [ ] 작가 자료 큐레이션에 "신규 자료 제보" 폼 (Firestore)

---

## 2026-05-07 (DAY 4, 추가) — BYOK + 무료 크레딧 50→5 + 발행 점검

### 🎯 오늘의 목표
- 운영자가 자기 돈을 거의 안 쓰고도 출시할 수 있는 비용 구조로 전환
- 발행 직전 코드/타입/테스트/빌드 일관성 보장 (회귀 0)

### 💸 비용 분석 → BYOK 도입 결정
- Gemini 2.5 Flash Image 단가: $0.039/image
- 32세트 1번 = $1.248 (₩1,660), 매출 ₩2,500 → 마진 ₩600~700 (PG 빼면 24~28%)
- **무료 50 크레딧 어뷰즈가 가장 큰 비용 변수**: 가입만 하고 시드 10번 시도 = $0.39 손실/명
- 무료 Gemini 키는 일일 25 RPD + 데이터 학습 사용으로 출시 부적합
- 결론: **운영자 첫 달 비용 ~$0** 만들기 위해 두 가지 동시 적용

### ✅ 완료 작업 (커밋 `3588d74`)

#### 1. 무료 크레딧 50 → 5
- `functions/src/onUserCreate.ts`: `SIGNUP_BONUS_CREDITS = 5` (시드 1번 체험)
- `firestore.rules`: `credits == 5` 보안 룰 갱신
- `components/AuthProvider.tsx`: 두 군데 fallback 5
- `app/page.tsx`, `app/auth/page.tsx`: 안내 문구 갱신

#### 2. BYOK (Bring Your Own Key) — 서버
- `functions/src/generateSticker.ts`:
  - 페이로드에 `byokApiKey?: string` 추가
  - `isValidByokKey()` — `AIza` + 30자 이상 영숫자 형식 검증
  - 유효 키면: 그 키로 GoogleGenAI 호출, **크레딧 차감 0**, audit `type: "byok"` 기록
  - 무효/없으면: 운영자 키 + 크레딧 차감 (기존 동작 유지)
  - 응답에 `byok: boolean` 플래그 포함

#### 3. BYOK — 클라이언트
- `lib/byok.ts` (신규): localStorage 기반 키 보관 + 약관 동의 + 마스킹 + 형식 검증
  - 서버에 영구 저장 X (Function 호출 시에만 페이로드)
  - `getActiveByokKey()` 헬퍼 — 동의 + 형식 둘 다 만족할 때만 활성
- `app/settings/page.tsx` (신규): 키 입력/약관/저장/삭제 + "왜 BYOK?" 가이드
- `app/generate/page.tsx`: 실호출 모드에서 **🔑 [내 키 ↔ 운영자 키]** 토글
- `lib/firebase/functions.ts`: `byokApiKey` 타입 노출, 응답에 `byok` 플래그

#### 4. NavBar / Sitemap 통합
- NavBar에 ⚙️ 설정 아이콘 추가 (BYOK 안내 툴팁)
- sitemap에 `/settings` 등록

### 🐛 outline 알고리즘 INF 처리 버그 수정
단위 테스트 9개를 추가하면서 발견:
- 표준 Felzenszwalb-Huttenlocher 1D DT가 `INF - INF = NaN`으로 발산
- 우리는 알파 마스크 기반이라 INF가 흔함 → **빈 마스크 / region 바깥 / 알파 임계값 미만** 케이스에서 outline이 깨짐
- 수정: 첫 유한 씨앗을 미리 찾고, INF 위치는 "포물선 없음"으로 스킵
- 이건 출시 전 발견 못 했으면 사용자가 "테두리가 이상하게 보여요" 신고할 버그

### 🧹 카운트 하드코딩 일관 정리
- `21개 플랫폼` → `25개` (또는 `${PLATFORMS.length}` 동적)
- `6개 플랫폼` → `9개` (resize.ts 기준)
- 적용 파일: app/page.tsx, components/Footer.tsx, app/layout.tsx, app/resources/page.tsx, app/tools/page.tsx, app/tools/resize/page.tsx, public/og-image.svg, README.md

### 🔍 발행 전 최종 점검 결과
| 검증 | 결과 |
|------|------|
| Next 타입체크 | ✅ 0 errors |
| Functions 타입체크 | ✅ 0 errors |
| Vitest | ✅ **19 / 19** (outline 단위 테스트 9개 신규) |
| Next 프로덕션 빌드 | ✅ **17 정적 페이지** (`/settings` 신규 포함) |
| JSZip 번들링 | ✅ chunk 정상 포함 |

### 📊 운영자 시나리오 비교

| 단계 | 운영자 키 모드 | BYOK 모드 |
|------|----------------|-----------|
| 가입 | 무료 5 크레딧 | 동일 |
| 시드 1장 | 5 크레딧 (체험 끝) | 무제한 |
| 32세트 | 100 크레딧 (₩2,500) | 무제한 |
| 운영자 부담 | ₩52~1,660/요청 | **₩0** |

→ 운영자는 첫 달부터 BYOK 사용자만 받고도 운영 가능. 매출 생기면 그때 운영자 키 + 크레딧 결제 흐름 활성화하는 단계적 전략.

### 📋 발행 직전 체크리스트
- [x] 코드 변경 완료 (커밋 `3588d74`)
- [x] origin/main에 push 완료
- [ ] **`firebase deploy --only firestore:rules,functions`** — 보안룰 + BYOK 분기 함수 반영
- [ ] (선택) `firebase functions:secrets:set GEMINI_API_KEY` — 운영자 키 등록 (BYOK만으로도 운영 가능)
- [ ] GitHub Pages 자동 배포 확인 (`.github/workflows`)

---

## 2026-05-05 (DAY 1) — 기획 + 목업 부트스트랩

### 🎯 오늘의 목표
- 아이디어를 PRD로 변환
- 사용자가 시각적으로 확인할 수 있는 목업 빌드
- GitHub 레포 연동

### ✅ 완료한 작업

#### 1. 아이디어 정리 & 방향 합의
- 사용자 요구사항 수집:
  - **타겟**: 일반인 (그림 못 그리는 사람 포함)
  - **기술 스택**: Firebase (Auth, Firestore, Storage, Functions)
  - **수익 모델**: 무료 + 크레딧
- 핵심 기능 3개 합의:
  1. AI 자연어 → 32종 생성
  2. 수동 에디터 + AI 보정/추천
  3. 멀티 플랫폼 자동 패키징 + 수익화 허브
- 차별화 포인트: "한 번 만들면 6개 플랫폼 자동 변환"

#### 2. 외부 리서치 (WebSearch 6회 병렬)
- **카카오 이모티콘 스튜디오**: 멈춤 32개 PNG 360x360, 150KB↓ / 움직임 24개 (PNG21+GIF3), 650KB↓
- **OGQ 마켓**: 740x640, 24개, 메인 240x240, 탭 96x74
- **LINE 크리에이터스**: 370x320 max, 35% 수수료, ¥120-610
- **미리캔버스 기여자**: 자유 사이즈 비독점 제공
- **Etsy 디지털 스티커**: 1000x1000+ 300dpi, $0.20/listing + 6.5%
- **AI 단가**: Nano Banana $0.039/image, Replicate SDXL $0.002~/image
- **Firebase**: $0.18/100K reads, $0.026/GB 저장, $0.12/GB egress

#### 3. PRD 작성
- 위치: `/Users/dawith/docs/prd/vibe-moji.md`
- 9개 섹션 + 부록 3개 완비
- 비용 시뮬레이션: 1,000 활성 사용자 = 약 $1,290/월 인프라 비용
- BEP: 월 500 유료 사용자 + ARPU ₩10,000

#### 4. 목업 프로젝트 부트스트랩
- 위치: `/Users/dawith/emoticon`
- 스택: Next.js 15 + React 19 + TypeScript + Tailwind + DaisyUI
- 커스텀 테마 `vibemoji` (핑크-퍼플-옐로 그라데이션)
- 디렉터리 구조 확정

#### 5. 핵심 화면 4종 구현 (목업)
| 화면 | 경로 | 주요 기능 |
|------|------|----------|
| 랜딩 | `/` | Hero + 3가지 기능 소개 + 6개 플랫폼 미리보기 + 가격표 |
| AI 생성 | `/generate` | 4단계 위저드 (입력→시드→32종→완료) + 진행도 애니메이션 + ZIP 다운로드 버튼 |
| 수동 에디터 | `/editor` | 360x360 캔버스 (포인터 이벤트) + 4개 도구 + 32 슬롯 그리드 + AI 보정 버튼 |
| 수익화 허브 | `/marketplace` | 6개 플랫폼 카드 + 정렬(쉬운/빠른/수익순) + 신청 단계 모달 + 딥링크 |

#### 6. 마스터 데이터 정리
- `lib/platforms.ts`에 6개 플랫폼 사양 + 32종 감정 라벨 정리
- 각 플랫폼: 사이즈, 장수, 포맷, 수수료, 심사기간, 난이도, 신청 링크, 단계 가이드, 메모

### 🚧 미구현 (의도적)
- Firebase 연동 (Auth, Firestore, Storage, Functions)
- 실제 AI 호출 (Nano Banana / Replicate)
- 크레딧 결제 (Toss Payments)
- 실제 ZIP 패키징 (sharp + jszip)
- 인증/회원가입 플로우

### 📝 의사 결정 메모
- **AI 모델 1순위**: Nano Banana 2 (캐릭터 일관성 + 단가 경쟁력)
- **정밀 모드 옵션**: Replicate SDXL+LoRA (사용자별 학습, P1)
- **카카오 자동 제출 ❌**: 공식 API 없음 → "패키징까지 자동, 업로드는 수동 가이드"
- **AI 생성물 정책**: 카카오 약관 명시 금지 아님, 단 "본인 제작" 요구 → 수동 에디터 1회 편집 권장 가이드 + 인증 뱃지

### 🔥 핵심 리스크
1. **캐릭터 일관성 실패** — Nano Banana 2 reference image 활용 + 재생성 무료 보장
2. **카카오 정책 변동** — 미승인 사례 수집 + 월 1회 가이드 업데이트
3. **Firebase egress 폭주** — 1,000 사용자 넘으면 Cloudflare R2 마이그레이션 필수

### 다음 액션
- [ ] `npm install && npm run dev`로 로컬 동작 확인
- [ ] Firebase 프로젝트 생성
- [ ] Gemini API 키 발급 + Nano Banana 호출 테스트
- [ ] 베타 랜딩 페이지에 대기열 폼 추가

---

## 2026-05-05 (DAY 1, 추가) — 일관성 평가 하네스 풀 세팅

### 🎯 추가 목표
- 캐릭터 일관성이 PRD 최대 리스크라 정량 평가 게이트를 본격 구축
- 모델/프롬프트 변경 시 회귀 즉시 탐지 가능한 인프라

### ✅ 완료한 작업

#### 1. 4 layer 하네스 구조 확립
| 레이어 | 파일 | 역할 |
|-------|------|------|
| Dataset | `evals/datasets/characters.yaml` | 5 + 1 adversarial 캐릭터 (시드 prompt + features 체크리스트) |
| Dataset | `evals/datasets/emotions.yaml` | 32 슬롯 감정/액션 description |
| Runner | `runners/mock.ts` | API 키 없이 검증 가능, 의도적 25% 실패 주입 |
| Runner | `runners/nano-banana.ts` | Gemini 2.5 Flash Image 실제 호출, reference image 지원 |
| Runner | `runners/replicate-sdxl.ts` | P1 정밀 모드 스텁 (fallback 활성화 인터페이스만) |
| Metric | `metrics/phash.ts` | 32x32 그레이스케일 perceptual hash + Hamming |
| Metric | `metrics/color-histogram.ts` | RGB 4-bin Bhattacharyya 거리 |
| Metric | `metrics/llm-judge.ts` | Gemini Vision 1-5점 judge + violated_features 추출 |
| Metric | `metrics/aggregate.ts` | 가중 평균 (LLM 0.5 / pHash 0.3 / color 0.2) |
| Reporter | `reporter/html.ts` | 캐릭터별 시드 + 32 변형 그리드, base64 임베드 단일 파일 |
| Reporter | `reporter/markdown.ts` | CI PR 코멘트용 |
| Orchestrator | `runner.ts` | 캐릭터별 시드 → 변형 32개 → 메트릭 → 집계 |
| CLI | `cli.ts` | `npm run eval -- --generator nano-banana --slots ...` |

#### 2. 통과 기준 (`thresholds.ts`)
- aggregate ≥ 0.80 (변형 1장 합격선)
- characterMean ≥ 0.83 (캐릭터 32장 평균)
- failureCountMax ≤ 4 (32장 중 12.5% 허용)
- llmJudgeMin ≥ 0.75 (= 4점/5점, 사람 인식 기준)

#### 3. 검증 (mock end-to-end)
- 6 캐릭터 × 4 슬롯 = 24 변형 생성 + 메트릭 측정 완료
- 의도적 실패 주입(mock 25%)이 정확히 잡힘:
  - 갈색 강아지 4/4 실패 → 캐릭터 ❌
  - 주황 고양이 2/4 실패 → 통과
  - 디테일 여우(adversarial) 1/4 실패 → 통과
- 전체 평균 0.875로 ❌ FAILED (강아지 캐릭터 평균 0.688이 게이트 트리거)
- HTML 리포트 419KB, Markdown 1.5KB 정상 생성

#### 4. Vitest 단위 테스트 (10/10 통과)
- pHash 동일/다른 패턴 점수 검증
- color-histogram 동일/반대색 검증
- aggregate 가중치 산식 검증
- 데이터셋 로드 검증 (32 슬롯 + adversarial 포함)
- MockGenerator 시드/변형 PNG 생성 검증

#### 5. CI/CD 워크플로
- `.github/workflows/consistency-eval.yml`
- PR 트리거: mock smoke + vitest (API 키 불필요)
- workflow_dispatch 트리거: Nano Banana 실제 호출 (1 캐릭터 5 슬롯)
- artifact 업로드로 HTML 리포트 보존

#### 6. 비용 가이드 명문화
| 시나리오 | 비용 |
|---------|------|
| Mock smoke | $0 |
| 1 캐릭터 5 슬롯 | $0.20 |
| 전체 5 + adversarial 캐릭터 × 32 슬롯 | $6.43 + $0.10 (LLM-judge) |
| LLM-judge 25% 샘플링 | $6.43 + $0.025 |

### 📝 의사 결정 메모
- **pHash + color-histogram 조합 vs CLIP**: 학습 모델 의존성 / 패키지 크기 / 빠른 CI 위해 pHash + 색 히스토그램으로 시작. CLIP embedding은 P1에서 별도 메트릭으로 추가 검토.
- **LLM-judge weight 0.5**: 자동 메트릭이 못 잡는 의미적 변화(귀 모양, 눈 위치, 식별 특징)를 잡는 게 핵심이라 가중치 가장 높게.
- **adversarial 캐릭터 1개 (디테일 여우)**: 디테일 많을수록 모델이 매번 다르게 그림 → 일관성 한계 탐색용. 향후 의상/소품 변화도 추가 케이스로 확장.
- **샘플링 옵션**: LLM-judge 비용 절감을 위해 `--llm-judge-rate 0.25` 플래그 제공. baseline 측정은 1.0, 회귀 검증은 0.25 권장.

### 🚧 추후 보강 사항
- CLIP embedding 메트릭 (P1)
- 두 run.json 비교 diff 도구 (회귀 자동 탐지)
- 사람 라벨링 UI (LLM-judge 보정용)
- Gemini 응답 변동성 완화 (다회 평균)

### 다음 액션
- [ ] GEMINI_API_KEY 발급 → `npm run eval -- --generator nano-banana --characters pink-rabbit --slots 1,5,17,25` 실제 검증
- [ ] 결과 점수에 따라 Nano Banana 단독 vs SDXL+LoRA fallback 결정

---

## 2026-05-06 (DAY 2) — 이비스페인트 레퍼런스 → 수동 에디터 본격 리팩토링

### 🎯 오늘의 목표
사용자 요청: "이비스페인트라는 어플의 기능도 레퍼런스 삼아서 가져와"
→ 카카오 이모티콘 작가들의 표준 도구인 이비스페인트의 핵심 기능을 우리 에디터에 압축 반영

### ✅ 적용된 이비스페인트 기능

#### 1. 브러시 프리셋 4종 (`_lib/brush-presets.ts`)
이모티콘 제작 95% 커버리지로 압축:
| 프리셋 | 용도 | alpha | composite | shadow | 권장 손떨림 |
|--------|------|-------|-----------|--------|------------|
| 🖋️ G펜 | 외곽선 전용, 단단한 검정선 | 1.0 | source-over | - | 3 |
| ✏️ 연필 | 초안 스케치, 흐릿한 텍스처 | 0.5 | source-over | - | 1 |
| 🖍️ 마커 | 채색용, 겹치면 진해지는 반투명 | 0.45 | multiply | - | 2 |
| 💨 에어브러시 | 볼터치/그라디언트, 부드러운 가장자리 | 0.15 | source-over | shadowBlur | 0 |

브러시 변경 시 권장 두께/손떨림 자동 적용.

#### 2. 손떨림 보정 (`_lib/stabilizer.ts`)
이비스페인트의 핵심 보조 기능 — Pull-string 방식 이동평균 (window 3~11).
사용자가 0~5 슬라이더로 직접 조절. 0=raw 입력, 5=가장 부드러움.

#### 3. 무제한 레이어 → 3 기본 + 최대 6 (`_lib/layers.ts`)
이비스페인트는 무제한이지만 카카오 이모티콘 작업 흐름상 3개로 충분:
- 선화 (G펜 전용)
- 채색 (마커/에어브러시)
- 배경 (참고용 / 텍스처)
+ 사용자 추가 가능 (최대 6, 초과 시 알림)

각 레이어:
- 가시성 토글 (👁️)
- 투명도 슬라이더 (활성 레이어만)
- 클리핑 마스크 토글 (✂️) — 아래 레이어 픽셀 있는 곳에만 표시
- 잠금 (locked, 미구현)
- 활성 레이어만 편집 가능

#### 4. Undo/Redo (`_lib/canvas-history.ts`)
스트로크 시작 시 모든 레이어 ImageData 스냅샷.
- 최대 30개 히스토리 (메모리 한도)
- 새 스트로크 시 redo 스택 초기화
- ↶ ↷ 헤더 버튼

#### 5. 자(Ruler) 도구
이비스페인트의 직선/원/사각 자를 단순화:
- 직선 (─)
- 사각 (▭)
- 타원 (○)
포인터 다운→무브 시 점선 미리보기, 업 시 실제 stroke 커밋.

#### 6. 사진 트레이스
이비스페인트의 참고 이미지 위에 트레이스하는 기능:
- 이미지 업로드 → 캔버스 가장 아래 표시
- 투명도 0~100% 슬라이더
- 원본 이미지는 export에 포함되지 않음 (display 합성에서만 그려짐)

#### 7. Surrounding Eraser (스텁)
이비스페인트의 v13 추가 기능 — 가장자리에서 안쪽으로 연속된 흰색을 자동 투명화.
실제 구현은 sharp + flood-fill 기반 Cloud Function 예정. 버튼만 노출.

#### 8. 타임랩스 녹화 (스텁)
이비스페인트의 핵심 마케팅 기능 — 그리는 과정을 자동 영상으로 저장.
인스타/스레드 자동 업로드 훅까지 연결 예정 (P2의 인스타 자동 포스팅과 결합).
헤더 ● REC 인디케이터 + 토글.

#### 9. 채우기 / 텍스트 / 지우개
- 채우기: source-atop으로 활성 레이어 픽셀이 있는 곳만 색 변경 (단순화 버전)
- 텍스트: 클릭 시 prompt → 위치에 입력
- 지우개: destination-out으로 알파 제거

#### 10. UI 레이아웃
이비스페인트의 좌측 도구 / 우측 레이어 / 하단 색·두께 구조 차용:
- **좌측**: 도구 6개 + Surrounding Eraser + 타임랩스 (모바일에서 그리드)
- **중앙**: 320px 디스플레이 캔버스 + 체커보드 배경 (투명 영역 시각화)
- **우측**: 레이어 패널 + 32 슬롯 패널
- **하단**: 브러시 프리셋 + 자 서브메뉴 + 색상 + 두께 + 손떨림 + 트레이스

### 🛠 의도적으로 단순화한 부분
- **47,000개 브러시 → 4종**: 이모티콘 제작에 90% 안 쓰임
- **27개 블렌드 모드 → 3종 (source-over, multiply, source-atop)**: UI 단순화
- **벡터 베지어 → 직선/원/사각 자만**: 카카오 이모티콘은 자유 곡선 위주
- **2,100개 폰트 → 시스템 sans-serif**: 향후 Pretendard 등 카카오 권장 폰트 추가
- **84개 필터 → 0개**: 필터는 AI 보정에서 처리

### 📦 추가된 파일
- `app/editor/page.tsx` — 전면 리팩토링 (260 → 580 lines)
- `app/editor/_lib/brush-presets.ts`
- `app/editor/_lib/stabilizer.ts`
- `app/editor/_lib/canvas-history.ts`
- `app/editor/_lib/layers.ts`

### 빌드/테스트 검증
- ✅ `npm run build` — 4 페이지 (/, /editor, /generate, /marketplace) 모두 정적 빌드 성공
- ✅ `npm run test` — vitest 10/10 (메트릭 + 데이터셋 + 런너) 통과
- ✅ `npm run eval` mock smoke — 24 변형 정상 평가, 의도된 실패 게이트 작동

### 다음 액션
- [ ] 실제 사용자 5명 시연 → 수동 에디터 인터랙션 피드백 수집
- [ ] Firebase 프로젝트 셋업 + Storage 연동 (세트 저장 동작)
- [ ] sharp 기반 멀티 플랫폼 자동 리사이즈 Cloud Function
- [ ] GEMINI_API_KEY 발급 → 실제 일관성 검증
- [ ] 타임랩스 녹화 실제 구현 (MediaRecorder API)

---

## 2026-05-06 (DAY 2, 추가) — 수익화 플랫폼 확장 (6개 → 21개)

### 🎯 목표
사용자 요청: "여기 다양한 플랫폼에 대응할 수 있도록 구성해줘" + 인스타 수익화 플랫폼 리스트 이미지 첨부

### ✅ 완료한 작업

#### 1. 플랫폼 데이터 4 카테고리로 재구성 (`lib/platforms.ts`)

**A. 메신저 이모티콘 (4개)** 💬 — 가장 큰 시장, 진입 어려움
- 카카오 멈춤, 카카오 움직임, OGQ 마켓, LINE 크리에이터스

**B. 요소·스톡 마켓 (7개)** 🎨 — PNG/벡터/템플릿 단위 판매, 비독점
- 미리캔버스, 툴디(Tooldi), 크라우드픽, Adobe Stock, Shutterstock, Creative Market, 포스타입

**C. POD 굿즈 (3개)** 🛍️ — 디자인만 올리면 자동 제작·배송
- 텐바이텐(10x10), 마플샵, Redbubble

**D. 디지털 크리에이터 (6개)** 💼 — 스티커팩 직접 판매
- Etsy, Gumroad, 위버딩, 하플, 크티, 그로블

총 **21개 플랫폼** (한국 13개 + 글로벌 7개 + 1개 양쪽)

#### 2. 플랫폼별 메타데이터 표준화
각 플랫폼에 다음 필드 통일:
- `category` — 4개 enum
- `mainSize`, `count`, `format` — 사양
- `commission`, `reviewDays`, `difficulty` — 비즈니스 정보
- `expectedRevenue` — 현실적 수익 추정
- `submitUrl` — 신청 페이지 직링크
- `steps[]` — 단계별 가이드
- `notes` — 주의사항/팁
- `global?: boolean` — 글로벌 시장 여부 (영문 SEO 필요)
- `nonExclusive?: boolean` — 비독점 가능 (다른 플랫폼 동시 등록)

#### 3. 마켓플레이스 페이지 UI 개편 (`app/marketplace/page.tsx`)
- **헤더 통계**: 4개 카테고리별 플랫폼 개수 한눈에
- **카테고리 탭**: 전체 / 메신저 / 스톡 / POD / 디지털 (개수 배지 포함)
- **글로벌 필터**: "🌍 글로벌만" 체크박스 (해외 플랫폼만 보기)
- **정렬**: 쉬운 순 / 빠른 순 / 카테고리 순
- **카드 뱃지**: 🌍 글로벌 / 비독점 표시
- **빈 결과 처리**: 필터 초기화 버튼
- **전략 팁**: "쉬운 플랫폼 먼저 → 카카오 도전" 가이드 명시

#### 4. 랜딩 페이지 카테고리 카드 (`app/page.tsx`)
21개를 한 줄에 펼치는 대신 4개 카테고리 카드로 재구성:
- 카테고리별 emoji + label + 설명 + 플랫폼 6개 미리보기 + "+N개" 표시
- "전체 21개 플랫폼 신청 가이드 보기 →" CTA

### 📝 의사 결정 메모
- **카테고리 구분 근거**: 사용자가 첨부한 이미지의 ❶❷❸ 분류(요소 판매 / POD / 크리에이터)에 메신저 이모티콘(원래 강점)을 추가해 4개로 확장.
- **카카오 단독 의존 분산**: 카카오 통과율 5% 미만이라 다른 카테고리 플랫폼 병행 필수. 마켓플레이스 알림에 명시.
- **비독점 표시**: 한 캐릭터를 여러 플랫폼에 동시 등록할 수 있는지가 작가 수익 다각화에 결정적이라 뱃지로 부각.
- **글로벌 필터**: 영문 SEO/세금 처리 필요한 플랫폼만 따로 보고 싶은 사용자 (Etsy/Gumroad/Adobe/Shutterstock/Creative Market/Redbubble/LINE) 빠른 접근.

### 🚧 향후 보강
- **플랫폼별 ZIP 자동 변환 매핑**: 현재 카카오/OGQ/LINE/Etsy 4종만 PRD에 정의 → POD용 7632x6480 등 추가 사이즈 변환 룰 작성
- **플랫폼 우선순위 추천 알고리즘**: 사용자가 "처음이에요" 선택 시 (크라우드픽 → 마플샵 → Gumroad → Etsy → OGQ → 카카오) 순서 자동 추천
- **수수료/세금 통합 계산기**: 플랫폼별 실수령액 시뮬레이션

---

## 2026-05-06 (DAY 2, 추가 2) — Firebase 백엔드 스캐폴드 + GH Pages 배포

### 🎯 목표
1. dawith0ai@gmail.com 계정으로 Firebase 프로젝트 생성하고 emoticon 레포에 연결
2. Auth/Firestore/Storage/Functions 풀 스캐폴드 (보안 룰 포함)
3. 사용자가 직접 확인할 수 있게 GH Pages로 정적 사이트 배포

### ✅ Firebase 셋업

#### 프로젝트 생성
- Firebase Project ID: `vibemoji-app`
- Console: https://console.firebase.google.com/project/vibemoji-app
- 소유자: dawith0ai@gmail.com

#### 설정 파일 (emoticon/)
- `.firebaserc`: 기본 프로젝트 vibemoji-app 연결
- `firebase.json`: Firestore + Storage + Functions + Hosting + Emulator 통합 설정
- `firestore.rules`: 사용자 데이터 격리 + 크레딧 클라이언트 조작 방지
- `firestore.indexes.json`: projects/transactions 복합 인덱스
- `storage.rules`: users/{uid}/projects 격리 + 5MB 이미지 제한 + public/ 읽기

#### Cloud Functions 스캐폴드 (functions/)
- `package.json`: firebase-admin, firebase-functions, @google/genai, sharp, archiver
- `tsconfig.json`: ES2022 CJS 빌드
- `src/admin.ts`: Firebase Admin SDK 초기화 헬퍼
- `src/onUserCreate.ts`: Auth 트리거 — 회원가입 시 50 크레딧 자동 지급 + 거래 기록
- `src/generateSticker.ts`: HTTPS Callable 2종
  - `generateSeedCharacter`: 자연어 → 시드 1장 (5 크레딧 차감, 트랜잭션)
  - `generateStickerSet`: 시드 + 32 감정 → 32장 일괄 (100 크레딧, 부분 실패 허용)
- `src/processStickerVariants.ts`: Storage 트리거 — `raw/*.png` 업로드 시 4개 플랫폼 사이즈 자동 변환
- `src/packagePlatform.ts`: HTTPS Callable — 플랫폼별 ZIP 패키징 + Signed URL 반환

#### 보안 룰 핵심 정책
- 사용자는 본인 데이터만 read/write
- `users/{uid}.credits` 클라이언트 직접 조작 차단 (Function 전용)
- `credits/transactions/items/*` 쓰기는 Function 전용, 읽기는 본인 거래만
- Storage 업로드: 본인 프로젝트 경로 + 5MB + 이미지 MIME만

### ✅ GH Pages 정적 배포

#### Next.js 정적 export 모드 추가 (`next.config.js`)
- 환경변수 `GITHUB_PAGES=true` 시 `output: "export"` + `basePath: "/emoticon"` + `assetPrefix` + `trailingSlash` 활성화
- 로컬 개발은 영향 없이 그대로 동작

#### TypeScript 분리 (`tsconfig.json`)
- `exclude`에 `functions`, `evals/runs` 추가 → Next.js 빌드 시 Functions 코드 타입체크 안 함

#### 빌드 검증
- `GITHUB_PAGES=true npm run build` → `out/` 디렉토리에 4 페이지 (index, editor, generate, marketplace) + `.nojekyll` + 자산 정적 생성

#### GitHub Actions (`.github/workflows/deploy-pages.yml`)
- main 브랜치 push 시 자동 트리거
- `npm ci → GITHUB_PAGES=true npm run build → upload-pages-artifact → deploy-pages`
- `actions/configure-pages@v5`, `actions/deploy-pages@v4` 사용

#### 레포 설정
- 레포 가시성: private → **public** 전환 (GH Pages free 플랜 요구)
- Pages 빌드 모드: `workflow` (Actions 기반)
- 배포 URL: **https://myjun090-spec.github.io/emoticon/**

### 📦 추가된 파일 요약
```
.firebaserc
firebase.json
firestore.rules
firestore.indexes.json
storage.rules
functions/
  package.json
  tsconfig.json
  src/index.ts
  src/admin.ts
  src/onUserCreate.ts
  src/generateSticker.ts
  src/processStickerVariants.ts
  src/packagePlatform.ts
.github/workflows/deploy-pages.yml
public/.nojekyll
```

### ⚠️ 발행 준비 체크리스트 (현재 상태 정직 평가)
| 영역 | 상태 | 비고 |
|------|------|------|
| UI 4페이지 | ✅ | 정적 빌드 가능 |
| 일관성 평가 하네스 | ✅ | mock + Nano Banana 어댑터 |
| Firebase 설정 + 보안 룰 | ✅ | 코드 작성 완료 (배포 미완) |
| Cloud Functions 코드 | ✅ | 4개 함수 작성 (배포 + Secret 등록 필요) |
| Firebase Auth 클라이언트 | ❌ | Next.js에 SDK 통합 필요 |
| 실제 AI 호출 동작 | ❌ | GEMINI_API_KEY Secret 등록 + Functions 배포 필요 |
| 결제 (Toss Payments) | ❌ | 미구현 |
| 약관/개인정보처리방침 | ❌ | 법무 검토 필요 |
| 도메인 / SSL | 🟡 | github.io 서브도메인 자동 |
| 분석/에러 모니터링 | ❌ | PostHog/Sentry 미연동 |

### 다음 액션 (MVP까지)
- [x] Firebase Auth 클라이언트 SDK 통합 (`lib/firebase/client.ts`)
- [x] 회원가입/로그인 UI 페이지 (`app/auth/page.tsx`)
- [ ] `firebase functions:secrets:set GEMINI_API_KEY` + 함수 배포
- [ ] generate 페이지에서 `httpsCallable("generateSeedCharacter")` 실제 호출
- [ ] editor 저장 → Storage 업로드 + Firestore 메타 기록
- [ ] Toss Payments 결제 위젯 통합 + 크레딧 충전 함수
- [x] 약관/PP 페이지 (footer 링크)

---

## 2026-05-07 (DAY 3) — 발행 95% 준비 + 컴플라이언스/SEO/모니터링 풀 스캐폴드

### 🎯 목표
"발행 전 95%까지" — 베타 오픈 가능한 수준의 컴플라이언스, 외부 통신, SEO, 에러 핸들링, 분석을 모두 깔기.

### ✅ 추가된 영역

#### 1. 법적 페이지 + 컴플라이언스
- `app/privacy/page.tsx` — 개인정보처리방침 (한국 PIPC 의무 항목 7개 섹션)
  - 수집 항목, 목적, 보유 기간, 제3자 제공(Google/Toss/PostHog/Sentry), 권리, 안전 조치, DPO
- `app/terms/page.tsx` — 이용약관 8조 (저작권, AI 생성물 책임, 환불 7일, 분쟁 관할)
- `components/CookieConsent.tsx` — PIPC 동의 배너 (필수만/모두 허용 + localStorage)
- 푸터에 약관/PP/이메일 링크 통합 (`components/Footer.tsx`)

#### 2. Firebase Functions/Storage 클라이언트 래퍼
- `lib/firebase/functions.ts` — `httpsCallable` 안전 래퍼
  - `CallableResult<T>` 패턴 (ok 분기로 UI에서 안전하게 처리)
  - `generateSeedCharacter`, `generateStickerSet`, `packagePlatform` 도메인 타입 정의
- `lib/firebase/storage.ts` — 캔버스 → Storage 업로드 헬퍼
  - `uploadStickerPng(uid, projectId, slot, label, blob)` 자동 경로 + signed URL
  - `canvasToBlob` 유틸 (HTMLCanvasElement → Blob 변환)

#### 3. 베타 대기열
- `components/WaitlistForm.tsx`
  - 이메일 + 관심사(작가/브랜드/팬덤) 수집
  - PIPC 동의 체크박스 강제
  - Firestore `public/waitlist/entries` 컬렉션에 저장
  - Firebase 미설정 시 데모 모드 안내
- 랜딩 페이지에 폼 + 7단계 베타 로드맵 추가

#### 4. SEO + PWA
- `app/sitemap.ts` — 7 라우트 자동 생성 (`force-static`)
- `app/robots.ts` — 기본 허용 + `/auth` 차단
- `public/manifest.webmanifest` — PWA 매니페스트, basePath 반영, 앱 단축키 3개
- `public/icon.svg`, `public/og-image.svg` — VibeMoji 그라데이션 아이콘 + 1200x630 OG 이미지
- `app/layout.tsx` 메타데이터 강화:
  - `metadataBase`, OpenGraph(ko_KR), Twitter card, canonical, manifest 링크
  - `viewport` 분리 export (Next 16 권장)
  - 키워드 7개 ("AI 이모티콘", "카카오 이모티콘", "Nano Banana" 등)

#### 5. 에러 핸들링 + 404
- `app/error.tsx` — 글로벌 에러 바운더리, digest 표시 + 다시 시도/홈
- `app/not-found.tsx` — 404 폴리시 + 4개 주요 페이지로 빠른 이동

#### 6. 분석 + 모니터링 (env-gated)
- `lib/analytics.ts` — PostHog 동적 import
  - `NEXT_PUBLIC_POSTHOG_KEY` + 쿠키 동의(analytics=true) 둘 다 있어야 활성화
  - 미설정/미동의 시 모든 호출 noop (정적 export 호환)
  - `trackEvent`, `identify`, `resetAnalytics` API
- `lib/sentry.ts` — Sentry 동적 import
  - `NEXT_PUBLIC_SENTRY_DSN` 미설정 시 console.error로 폴백
  - `reportError(err, context)` 단일 API

#### 7. 환경변수 정리 (`.env.example`)
- AI / Firebase / 분석 / 배포 4개 섹션으로 분리
- 모든 항목에 발급 URL + 비고 주석

### 🛠 빌드 검증
- `npm run build` (GITHUB_PAGES=true) — 9 페이지 + sitemap + robots 정적 export ✅
- `npm run test` — vitest 10/10 통과 ✅
- 모든 신규 컴포넌트는 클라이언트 사이드 동적 import + env 가드로 정적 export 호환

### 📦 추가된 파일 (DAY 3)
```
app/privacy/page.tsx
app/terms/page.tsx
app/sitemap.ts
app/robots.ts
app/error.tsx
app/not-found.tsx
components/WaitlistForm.tsx
components/CookieConsent.tsx
components/Footer.tsx
lib/firebase/functions.ts
lib/firebase/storage.ts
lib/analytics.ts
lib/sentry.ts
public/manifest.webmanifest
public/icon.svg
public/og-image.svg
.env.example  (재구성)
```

### ⚠️ 발행 95% 체크리스트 (현재 상태)
| 영역 | 상태 | 비고 |
|------|------|------|
| UI 4 페이지 + 인증 + 약관/PP | ✅ | 9 페이지 정적 export |
| 일관성 평가 하네스 | ✅ | mock + Nano Banana 어댑터 |
| Firebase 설정 + 보안 룰 | ✅ | rules + indexes 코드 작성 완료 |
| Cloud Functions 코드 | ✅ | 4개 함수 작성 |
| Firebase Auth 클라이언트 | ✅ | AuthProvider + AuthStatus + /auth |
| Firestore 클라이언트 (초안 저장) | ✅ | generate에서 작성 중 자동 저장 |
| Functions 호출 래퍼 | ✅ | `lib/firebase/functions.ts` |
| Storage 업로드 헬퍼 | ✅ | `lib/firebase/storage.ts` |
| 베타 대기열 폼 | ✅ | Firestore 저장 (Firebase env 필요) |
| 쿠키 동의 (PIPC) | ✅ | localStorage 기반 배너 |
| SEO (sitemap/robots/OG/manifest) | ✅ | 정적 생성 |
| 404 + Error 바운더리 | ✅ | 폴리시 완료 |
| 분석/모니터링 | ✅ (스캐폴드) | 환경변수 + 동의 시 활성화 |
| **실제 AI 호출 동작** | 🟡 | 코드 준비됨, Functions 배포 + GEMINI_API_KEY Secret 필요 |
| **실제 Storage 저장 동작** | 🟡 | 코드 준비됨, Firebase env 클라이언트 등록 필요 |
| **결제 (Toss Payments)** | ❌ | 미구현 — 베타에서는 무료 크레딧만 |
| **도메인 / 커스텀 SSL** | 🟡 | github.io 자동 |
| 알림 (이메일) | ❌ | Resend 미연동 |

### 다음 액션 (베타 오픈까지 5%)
- [x] `firebase apps:create WEB VibeMoji` → SDK 설정 가져오기
- [x] `.env.local` 채우기 + GH Pages secrets 6개 등록
- [x] `firebase deploy --only firestore:rules,firestore:indexes` 라이브 배포
- [x] generate 페이지에서 `generateSeedCharacter` 실호출 분기 추가 (mock/real 토글)
- [x] editor "Firebase에 저장" 버튼 활성화 (uploadStickerPng 호출)
- [x] /debug 페이지로 7개 헬스체크 자동화
- [ ] **사용자 콘솔 클릭 4종** (SETUP-CHECKLIST.md 참고)
- [ ] `firebase functions:secrets:set GEMINI_API_KEY` + `firebase deploy --only functions`
- [ ] 베타 100명 모집 → 실사용 피드백
- [ ] (P1) Toss Payments 결제 위젯

---

## 2026-05-07 (DAY 3, 추가) — Firebase 라이브 연동 + 디버그 인프라

### 🎯 사용자 요구
1. Firebase 프로젝트(`vibemoji-app`)와 emoticon 레포 실연결
2. "세부 디버깅도 확실하게" — 정량 검증 가능한 헬스체크 인프라

### ✅ 완료 작업

#### Firebase 웹 앱 등록
- `firebase apps:create WEB VibeMoji` → App ID `1:885797621275:web:e13aea4e230b007ea972be`
- `firebase apps:sdkconfig` → 6개 NEXT_PUBLIC_FIREBASE_* 키 획득
- `.env.local` 작성 (gitignored)
- GitHub repo secrets 6개 등록 (`gh secret set NEXT_PUBLIC_FIREBASE_*`)
- `.github/workflows/deploy-pages.yml` 빌드 단계에 secrets 주입 → 정적 export에 포함

#### Firestore 룰 + 인덱스 라이브 배포
- `firebase deploy --only firestore:rules,firestore:indexes` ✅
- 보안 룰 적용: 사용자 격리 + 크레딧 클라이언트 조작 방지
- 복합 인덱스 적용: projects(status,updatedAt), transactions(userId,createdAt)

#### generate 페이지 실호출 분기
- `callMode` 토글 (mock | real) 추가
- real 모드: `generateSeedCharacter` / `generateStickerSet` httpsCallable 직접 호출
- 시간순 호출 로그 (최근 30개) + 에러 코드별 상세 표시
- not-found / unauthenticated / invalid-argument 분기 처리

#### editor PNG 업로드
- `saveSet` → Firestore 프로젝트 생성 + 활성 슬롯 PNG를 Storage에 업로드
- 동적 import 사용으로 정적 export 호환 (`@/lib/firebase/storage`)
- 업로드 결과 (성공/실패 카운트) UI에 표시

#### `/debug/` 헬스체크 페이지 (신규)
7개 자동 검사:
1. **env**: NEXT_PUBLIC_FIREBASE_* 6개 변수 존재 확인
2. **app**: Firebase App 초기화 검증
3. **auth**: Auth SDK 인스턴스 + 세션 인식
4. **firestore-read**: users/me 또는 platforms 읽기 (룰 통과)
5. **firestore-write**: users/me/projects 임시 생성 + 삭제 (write 룰 검증)
6. **storage**: users/{uid}/profile 에 PNG 업로드 + 다운로드 URL 획득
7. **functions-ping**: callable 통신 (not-found = 미배포 신호)

각 검사:
- 시작/종료 지연시간 측정
- 에러 객체 정규화 (name + message)
- 통과/실패 통계 + 검사별 상태 아이콘
- 로그인 시 자동 실행

#### SETUP-CHECKLIST.md
사용자 마지막 5%를 위한 단계별 콘솔 클릭 가이드:
1. Email/Password Provider 활성화
2. Authorized domain 추가 (`myjun090-spec.github.io`)
3. Storage 초기화 + 룰 배포
4. Blaze 플랜 업그레이드
5. GEMINI_API_KEY + Functions 배포
6. /debug/ 7/7 통과 확인

### ⚠️ 막힌 지점 (사용자 액션 필요)
- **Functions 배포**: Blaze 플랜 필수 → 결제 수단 등록 필요
- **Storage 초기화**: 콘솔에서 1클릭 (CLI로 자동화 불가)
- **Auth providers**: Email/Password 활성화 콘솔 클릭

### 📊 라이브 검증 (2026-05-07 시점)
- Pages URL: https://myjun090-spec.github.io/emoticon/ ✅ 200
- Debug page: /debug/ ✅ 200
- Firebase API key 정적 번들 포함 확인 (chunks/0wr0-s3rcv0rr.js)
- vitest 10/10 통과 ✅
- npm run build (10 페이지 정적 export) ✅

---

## 2026-05-07 (DAY 3, 추가 2) — 베타 셋업 자동화 시도 + 결제 게이트 검증

### 🎯 사용자 요구
"일단 베타 서비스까지만 하게끔 세팅해놔"  
→ Blaze 결제 없이 가능한 한 모든 것을 자동 셋업.

### ✅ 자동 처리 완료 (REST API)
- `firebase-tools.json`에서 OAuth 토큰 추출 (dawith0ai@gmail.com, 약 44분 유효)
- Service Usage API로 `identitytoolkit.googleapis.com` 활성화
- Service Usage API로 `firebasestorage.googleapis.com` 활성화

### 🚧 결제 게이트 발견 (Blaze 강제 항목)
시도 + 결과:
| API | 결과 |
|-----|------|
| `POST /identityPlatform:initializeAuth` | **400 BILLING_NOT_ENABLED** |
| `POST /firebasestorage.../defaultBucket` | **403 PERMISSION_DENIED** |
| `POST /storage.googleapis.com/storage/v1/b` | **403 billing account in state absent** |

→ Identity Platform 무료 경로 자동화 불가 결론. 콘솔 `[Get started]` 1클릭 = 무료 티어 유일한 부트스트랩.

### 🛠 폴링 자동화 (`scripts/setup-firebase.mjs`)
1클릭만 사용자에게 맡기고 나머지는 자동으로 처리하는 스크립트:
- `dawith0ai@gmail.com` 토큰 자동 로드
- 필수 API 자동 활성화
- 미초기화 시 콘솔 URL 자동 `open` (macOS)
- **10초 간격 30회 (5분) GET /admin/v2/projects/.../config 폴링**
- `CONFIGURATION_NOT_FOUND` 응답이면 대기, 200이면 즉시 진행
- 감지 직후 Email/Password 활성화 + authorized domains 패치 (myjun090-spec.github.io + localhost)
- Storage는 Blaze 필요 → 베타에서는 스킵 + 안내
- 마지막 단계에서 라이브 URL과 다음 액션 가이드 출력

### 📦 SETUP-CHECKLIST.md 갱신
- 자동/수동 매트릭스 명시
- 베타 vs Blaze 두 경로 분리:
  - 🅰️ 무료 (1클릭 + 1줄): Auth + Firestore만 동작
  - 🅱️ Blaze (결제 + 자동 전부): Storage + Functions 추가 동작

### 📊 베타 오픈 시점 동작 매트릭스
| 기능 | 상태 |
|------|------|
| 회원가입/로그인 (Email/Password) | ✅ |
| Firestore 사용자 문서 자동 생성 + 50 크레딧 | ✅ |
| 21개 플랫폼 신청 가이드 + 딥링크 | ✅ |
| 수동 에디터 (4 브러시 + 레이어 + 자 + 트레이스) | ✅ |
| 베타 대기열 폼 (Firestore 저장) | ✅ |
| 쿠키 동의 + 약관/PP | ✅ |
| 디버그 헬스체크 (/debug) | ✅ |
| 정적 export GH Pages 자동 배포 | ✅ |
| AI 실호출 (Nano Banana) | 🟡 Blaze 후 |
| Storage PNG 업로드 | 🟡 Blaze 후 |
| 멀티 플랫폼 ZIP | 🟡 Blaze 후 |

### 다음 사용자 액션 (베타 오픈 30초)
1. https://console.firebase.google.com/project/vibemoji-app/authentication
2. **[Get started]** 클릭
3. (선택) `node scripts/setup-firebase.mjs` 재실행 → 자동으로 Email/Password 활성화 + 도메인 추가
